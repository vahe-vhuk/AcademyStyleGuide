
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Define the text you want to render


// Render the App component with the specified text
ReactDOM.render(
  <React.StrictMode>
    <App/>
  </React.StrictMode>,
  document.getElementById('root')
);